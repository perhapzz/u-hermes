# Hermes Web UI -- minimal Python dependencies
# The server uses PyYAML plus cryptography for optional local passkey/WebAuthn support.
# All heavy ML/agent deps live in the Hermes agent venv.
pyyaml>=6.0
cryptography>=42.0

# ---- Gateway / browser-dialog deps ----
# websockets: required by tools.browser_dialog_tool AND by Feishu websocket mode.
# aiohttp / lark-oapi / qrcode: required by Feishu + Weixin gateway adapters.
# Kept here (not in agent extras) so they install on every fresh USB setup
# without needing pip install hermes-agent[messaging,feishu].
websockets>=12.0
aiohttp>=3.10
lark-oapi>=1.5.3
qrcode>=7.4.2
